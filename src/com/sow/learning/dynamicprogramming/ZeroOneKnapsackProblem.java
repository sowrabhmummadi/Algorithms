package com.sow.learning.dynamicprogramming;

/**
 * Solution for 0/1 knapsack problem using the dynamic programming
 *
 * <P>
 * Given two integer arrays value[0,...n-1] and weights[0...n-1] with n-items. Also given an integer W which
 * represents knapsack capacity. Find out the maximum value subset of val[] such that sum of the weights of this subset
 * is smaller than or equal to W. you cannot break an item , either pick the complete item or don't pick it (0-1 property)
 * <P>
 */
public class ZeroOneKnapsackProblem {
}
